<?xml version="1.0" encoding="utf-8"?>
<sqlMapper namespace="mapper.user">
    <select  id="getUserByUsername">
        select * from im_user where username=#{username}
    </select>
    <select  id="getUserByUserId">
        select * from im_user where user_id=#{userId}
    </select>
</sqlMapper>